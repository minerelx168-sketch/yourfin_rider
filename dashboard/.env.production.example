# Dashboard build-time env (Vite bakes this into the static bundle).
#
# Same-origin behind the nginx proxy (docker-compose.prod.yml) — recommended:
VITE_API_URL=/api
#
# Or point directly at a separately-hosted API (then set CORS_ORIGINS on the API):
# VITE_API_URL=https://api.yourdomain.com/api
